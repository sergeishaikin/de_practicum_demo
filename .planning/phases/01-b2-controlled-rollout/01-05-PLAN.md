---
phase: 01-b2-controlled-rollout
plan: 05
type: execute
wave: 5
depends_on: [01-04]
files_modified:
  - artifacts/b2-rollout/05-cutover-runtime.txt
  - artifacts/b2-rollout/05-cutover-tests.log
  - artifacts/b2-rollout/05-cutover-receipt.json
  - artifacts/b2-rollout/05-rollback-receipt.json
autonomous: true
requirements: [CUT-02]
must_haves:
  truths:
    - "Gold switches to persisted Silver only after the green Plan 04 M5 receipt."
    - "The live post-cutover runtime is SILVER_MODE=b2, GOLD_SOURCE=persisted_silver, SHADOW_COMPARE=1."
    - "Shadow comparison stays enabled and a cutover failure restores Gold to legacy without resetting Silver or progress."
  artifacts:
    - path: "artifacts/b2-rollout/05-cutover-runtime.txt"
      provides: "Post-cutover container configuration and service logs"
      contains: "SILVER_MODE=b2, GOLD_SOURCE=persisted_silver, SHADOW_COMPARE=1"
    - path: "artifacts/b2-rollout/05-cutover-receipt.json"
      provides: "Persisted-Silver cutover and shadow observation receipt"
      contains: "M5 gate reference and rollback_verified"
    - path: "artifacts/b2-rollout/05-rollback-receipt.json"
      provides: "Conditional rollback proof for cutover failure"
      contains: "Gold source restored to legacy without Silver/progress reset"
  key_links:
    - from: "artifacts/b2-rollout/04-m5-gate-result.json"
      to: "docker-compose.extended.yml"
      via: "explicit precondition before environment transition"
      pattern: "passed=true"
    - from: "docker-compose.extended.yml"
      to: "de-demo-iceberg-medallion"
      via: "persisted-Silver Gold environment"
      pattern: "GOLD_SOURCE=persisted_silver|SHADOW_COMPARE=1"
---

<objective>
Switch Gold to persisted Silver under the green M5 gate and verify the reversible cutover.

Purpose: Make the approved architecture live only after objective evidence, while preserving shadow comparison and the existing legacy rollback path.
Output: Cutover runtime receipt, live/test verification, and a rollback receipt if any gate fails.
</objective>

<execution_context>
@C:/Users/serge/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/serge/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-b2-controlled-rollout/CONTEXT.md
@.planning/phases/01-b2-controlled-rollout/01-04-PLAN.md
@artifacts/b2-rollout/04-m5-cutover-evidence.json
@artifacts/b2-rollout/04-m5-gate-result.json
@docs/remediation/M4-shadow-gold-cutover.md
@docs/remediation/M5-fitness-functions-and-cutover-gates.md
@iceberg/common/cutover.py
@tests/integration/test_m4_gold_cutover.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce the green M5 gate and perform the persisted-Silver cutover</name>
  <files>artifacts/b2-rollout/05-cutover-runtime.txt</files>
  <action>Read artifacts/b2-rollout/04-m5-gate-result.json and stop before any restart unless passed=true, failed_checks=[], and every evaluator check is true. If the precondition is false, record the refusal and restore SILVER_MODE=legacy, GOLD_SOURCE=legacy, SHADOW_COMPARE=0; do not attempt cutover. If the precondition is green, set only SILVER_MODE=b2, GOLD_SOURCE=persisted_silver, and SHADOW_COMPARE=1 in the Compose process environment, run docker compose --env-file .env -f docker-compose.yml -f docker-compose.extended.yml config --quiet, and recreate only iceberg-medallion. Capture the exact container environment, Compose status, and first successful post-cutover logs in 05-cutover-runtime.txt. Do not disable shadow comparison, change B2 progress, delete outbox records, reset Iceberg tables, or alter physical layout/orchestration.</action>
  <verify>
    <automated>python -c "import json; p=json.load(open('artifacts/b2-rollout/04-m5-gate-result.json')); assert p['passed'] is True and p['failed_checks']==[]; print('M5 precondition PASS')"; $env:SILVER_MODE='b2'; $env:GOLD_SOURCE='persisted_silver'; $env:SHADOW_COMPARE='1'; docker compose --env-file .env -f docker-compose.yml -f docker-compose.extended.yml config --quiet; docker inspect de-demo-iceberg-medallion --format '{{range .Config.Env}}{{println .}}{{end}}' | Select-String 'SILVER_MODE=b2|GOLD_SOURCE=persisted_silver|SHADOW_COMPARE=1'</automated>
  </verify>
  <done>The service is recreated with exactly b2/persisted_silver/1 only after the green M5 receipt; a failed precondition leaves or restores the legacy configuration.</done>
</task>

<task type="auto">
  <name>Task 2: Verify cutover shadow safety and preserve rollback</name>
  <files>artifacts/b2-rollout/05-cutover-tests.log, artifacts/b2-rollout/05-cutover-receipt.json, artifacts/b2-rollout/05-rollback-receipt.json</files>
  <action>Run python -m pytest -q -m integration --basetemp .pytest-m5-cutover tests/integration/test_m4_gold_cutover.py -s and capture the test output; it must cover persisted-Silver Gold, shadow validation, late/lower-version behavior, and rollback to legacy without a new Silver snapshot. Query recent marts.lakehouse_metrics rows for source='medallion' and require successful status, shadow_comparisons greater than zero, shadow_mismatches=0, ff14_conflicts=0, and work_in_flight=0; write those observations plus the M5 gate reference and runtime values into 05-cutover-receipt.json. If any post-cutover shadow mismatch, FF-14, Gold equivalence loss, recovery anomaly, stale progress, failed test, or invalid runtime value occurs, stop the service with docker compose --env-file .env -f docker-compose.yml -f docker-compose.extended.yml stop iceberg-medallion, set GOLD_SOURCE=legacy while retaining SILVER_MODE=b2 and SHADOW_COMPARE=1 when B2 Silver is healthy, and run docker compose --env-file .env -f docker-compose.yml -f docker-compose.extended.yml up -d --no-deps --force-recreate iceberg-medallion; if B2 Silver is unhealthy, set SILVER_MODE=legacy, GOLD_SOURCE=legacy, SHADOW_COMPARE=0 and run the same Compose command. Record the post-rollback environment and unchanged Silver/progress identifiers in 05-rollback-receipt.json. Never repair a failure by resetting tables or progress.</action>
  <verify>
    <automated>python -m pytest -q -m integration --basetemp .pytest-m5-cutover tests/integration/test_m4_gold_cutover.py -s; docker exec de-demo-postgres psql -U app -d dwh -At -F '|' -c "select count(*),coalesce(sum(shadow_comparisons),0),coalesce(sum(shadow_mismatches),0),coalesce(sum(ff14_conflicts),0),coalesce(max(work_in_flight),0) from marts.lakehouse_metrics where source='medallion' and status='success'"</automated>
  </verify>
  <done>Persisted-Silver Gold is live under shadow comparison with zero mismatch/FF-14/stuck-work signals and a fresh rollback regression pass; any failure has a durable rollback receipt that preserves Silver and progress.</done>
</task>

<task type="auto">
  <name>Task 3: Machine-check the active cutover tuple and receipt</name>
  <files>artifacts/b2-rollout/05-cutover-receipt.json, artifacts/b2-rollout/05-cutover-runtime.txt</files>
  <action>After the cutover service recreation, run the exact command `docker compose --env-file .env -f docker-compose.yml -f docker-compose.extended.yml up -d --no-deps --force-recreate iceberg-medallion` only if the M5 receipt is green. Inspect the running container and write cutover_succeeded=true, runtime={SILVER_MODE:b2,GOLD_SOURCE:persisted_silver,SHADOW_COMPARE:1}, and shadow_comparisons>0 with shadow_mismatches=0 into 05-cutover-receipt.json. A rollback-only result sets cutover_succeeded=false and blocks telemetry.</action>
  <verify><automated>$actual = docker inspect de-demo-iceberg-medallion --format '{{json .Config.Env}}' | ConvertFrom-Json; if (($actual -notcontains 'SILVER_MODE=b2') -or ($actual -notcontains 'GOLD_SOURCE=persisted_silver') -or ($actual -notcontains 'SHADOW_COMPARE=1')) { throw 'active persisted-Silver tuple mismatch' }; python -c "import json; p=json.load(open('artifacts/b2-rollout/05-cutover-receipt.json')); assert p['cutover_succeeded'] is True and p['runtime']=={'SILVER_MODE':'b2','GOLD_SOURCE':'persisted_silver','SHADOW_COMPARE':'1'}"</automated></verify>
  <done>The active runtime and durable receipt prove a successful persisted-Silver cutover, not merely a rollback attempt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| M5 gate receipt -> operator shell | A machine-readable pass authorizes an irreversible serving-source change. |
| Operator environment -> medallion | Gold source and shadow settings select the serving path. |
| Persisted Silver -> Gold writer | Gold consumes committed Silver only after comparison. |
| Rollback command -> persistent state | Recovery must change configuration without destructive data repair. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Elevation of Privilege | Cutover authorization | mitigate | Refuse cutover unless the Plan 04 evaluator receipt is green with no failed checks. |
| T-01-02 | Tampering | Shadow comparison | mitigate | Keep SHADOW_COMPARE=1 and fail before Gold write on any mismatch. |
| T-01-03 | Denial of Service | Persisted-Silver cutover | mitigate | Roll back Gold to legacy on failure; preserve B2/Silver/progress state. |
| T-01-04 | Repudiation | Rollback evidence | mitigate | Capture pre/post Silver snapshot and progress identifiers in the rollback receipt. |
| T-01-SC | Tampering | Package installation | accept | No package installation is planned; unexpected installation blocks cutover. |
</threat_model>

<verification>
<automated>python -m pytest -q --basetemp .pytest-m5-cutover tests/test_m5_fitness_functions.py; python -m pytest -q -m integration --basetemp .pytest-m5-cutover-live tests/integration/test_m4_gold_cutover.py -s; docker inspect de-demo-iceberg-medallion --format '{{range .Config.Env}}{{println .}}{{end}}'</automated>
</verification>

<success_criteria>
Gold is selected from persisted Silver only after the green M5 receipt, the live configuration is b2/persisted_silver/1, shadow comparison remains enabled with zero mismatches, and the documented rollback path has been verified without resetting Silver or progress.
</success_criteria>

<output>
Create .planning/phases/01-b2-controlled-rollout/01-05-SUMMARY.md when done.
</output>
