---
phase: 01-b2-controlled-rollout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - artifacts/b2-rollout/01-preflight-runtime.txt
  - artifacts/b2-rollout/01-preflight-handoff.json
  - artifacts/b2-rollout/01-preflight-dbt.log
  - artifacts/b2-rollout/01-preflight-gate.json
autonomous: true
requirements: [CAN-03]
must_haves:
  truths:
    - "The persistent stack is frozen in the verified S1.2B handoff state before any B2 canary configuration is applied."
    - "The preflight receipt proves SAFE_STALE=0, LIVE_POST_MIGRATION=255, IN_FLIGHT_BLOCKED=0, BLOCKED=0, Bronze/Silver=218961/218961, Silver equals the accepted B2 projection, and dbt is 26/26 PASS."
    - "No writer, medallion, Gold, Silver, progress, physical layout, or orchestration change is made by preflight."
  artifacts:
    - path: "artifacts/b2-rollout/01-preflight-handoff.json"
      provides: "Read-only legacy outbox and Bronze/Silver handoff receipt"
      contains: "live_post_migration=255 and all blocked counts zero"
    - path: "artifacts/b2-rollout/01-preflight-dbt.log"
      provides: "Durable dbt semantic validation output"
      contains: "26/26 passing tests"
    - path: "artifacts/b2-rollout/01-preflight-gate.json"
      provides: "Machine-readable preflight decision"
      contains: "passed=true"
  key_links:
    - from: "scripts/classify_legacy_outbox.py"
      to: "artifacts/b2-rollout/01-preflight-handoff.json"
      via: "read-only live classifier receipt"
      pattern: "--evidence artifacts/b2-rollout/01-preflight-handoff.json"
    - from: "artifacts/b2-rollout/01-preflight-handoff.json"
      to: "artifacts/b2-rollout/01-preflight-gate.json"
      via: "exact baseline assertions"
      pattern: "SAFE_STALE|LIVE_POST_MIGRATION|silver_equals_b2_projection"
---

<objective>
Freeze and prove the verified S1.2B baseline before changing the runtime rollout mode.

Purpose: Establish a durable, read-only starting point so every later canary and cutover decision can be compared with the known state.
Output: Preflight runtime/status capture, handoff classifier receipt, dbt receipt, and a machine-readable gate that passes only for the exact required values.
</objective>

<execution_context>
@C:/Users/serge/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/serge/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-b2-controlled-rollout/CONTEXT.md
@docs/remediation/S1.2-legacy-outbox-handoff.md
@docs/remediation/M5-fitness-functions-and-cutover-gates.md
@docs/observability/O1-runtime-observability.md
@scripts/classify_legacy_outbox.py
@scripts/validate_runtime_config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture the frozen legacy runtime and service boundary</name>
  <files>artifacts/b2-rollout/01-preflight-runtime.txt</files>
  <action>Run from the repository root without changing .env or any source file. Use the existing Compose arguments --env-file .env -f docker-compose.yml -f docker-compose.extended.yml. Run python scripts/validate_runtime_config.py --env-file .env --profile local, docker compose --env-file .env -f docker-compose.yml -f docker-compose.extended.yml config --quiet, docker compose --env-file .env -f docker-compose.yml -f docker-compose.extended.yml ps -a, and docker inspect de-demo-iceberg-medallion --format '{{range .Config.Env}}{{println .}}{{end}}'. Capture the commands, exit codes, container status, and the observed SILVER_MODE=legacy, GOLD_SOURCE=legacy, SHADOW_COMPARE=0 values in the runtime receipt. Do not use stack reset, table drops, progress deletion, outbox deletion, or service rebuilds; preflight is read-only because it must not alter the baseline it is proving.</action>
  <verify>
    <automated>python scripts/validate_runtime_config.py --env-file .env --profile local; docker compose --env-file .env -f docker-compose.yml -f docker-compose.extended.yml config --quiet; docker inspect de-demo-iceberg-medallion --format '{{range .Config.Env}}{{println .}}{{end}}'</automated>
  </verify>
  <done>Runtime validation and Compose config pass; the receipt records the running legacy/legacy/0 configuration and no preflight mutation command was used.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the exact S1.2B handoff and dbt baseline</name>
  <files>artifacts/b2-rollout/01-preflight-handoff.json, artifacts/b2-rollout/01-preflight-dbt.log, artifacts/b2-rollout/01-preflight-gate.json</files>
  <action>Set the existing local catalog variables used by the S1.2 handoff procedure (ICEBERG_CATALOG_URI=http://localhost:18181, S3_ENDPOINT=http://localhost:19000, AWS_ACCESS_KEY_ID=minio, and AWS_SECRET_ACCESS_KEY=minio123, or the already-configured equivalent values from the active .env without committing secrets). Run python scripts/classify_legacy_outbox.py --evidence artifacts/b2-rollout/01-preflight-handoff.json; then run the documented dbt commands python -m dbt parse --project-dir dbt --profiles-dir dbt, python -m dbt compile --project-dir dbt --profiles-dir dbt, python -m dbt run --project-dir dbt --profiles-dir dbt --select semantic, and python -m dbt test --project-dir dbt --profiles-dir dbt, capturing all output in artifacts/b2-rollout/01-preflight-dbt.log. Create 01-preflight-gate.json only after asserting SAFE_STALE=0, LIVE_POST_MIGRATION=255, IN_FLIGHT_BLOCKED=0, BLOCKED=0, authoritative Bronze rows=218961, authoritative Silver rows=218961, Bronze and Silver NULL business_version rows=0, Silver unique order IDs=true, Silver equals the accepted B2 projection=true, and dbt has exactly 26 PASS with zero WARN/ERROR. A mismatch fails the gate and stops the phase before any B2 configuration change.</action>
  <verify>
    <automated>python scripts/classify_legacy_outbox.py --evidence artifacts/b2-rollout/01-preflight-handoff.json; python -m dbt test --project-dir dbt --profiles-dir dbt; python -c "import json; p=json.load(open('artifacts/b2-rollout/01-preflight-handoff.json')); assert (p['safe_stale'],p['live_post_migration'],p['in_flight_blocked'],p['blocked'])==(0,255,0,0); assert (p['authoritative_bronze_rows'],p['authoritative_silver_rows'])==(218961,218961); assert p['silver_equals_b2_projection'] is True; print('preflight handoff gate inputs PASS')"</automated>
  </verify>
  <done>All exact preflight values are durably recorded and 01-preflight-gate.json is passed; otherwise no later plan may execute.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator shell -> Compose/services | Environment and control commands can change runtime state. |
| Classifier -> Iceberg REST/MinIO | Read-only evidence crosses into the persistent data plane. |
| dbt client -> Trino/Iceberg | Validation queries read serving data and semantic contracts. |
| Evidence files -> later rollout gates | Local receipts become inputs to irreversible configuration decisions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | Runtime configuration | mitigate | Capture container environment and require legacy/legacy/0 before proceeding. |
| T-01-02 | Tampering | Handoff evidence | mitigate | Use the existing read-only classifier and assert every exact baseline field before writing the gate. |
| T-01-03 | Denial of Service | Persistent stack | mitigate | Do not reset, delete, rebuild, or mutate data during preflight; stop on any unexpected service change. |
| T-01-SC | Tampering | Package installation | accept | No package installation is planned; an unexpected install is outside scope and blocks execution. |
</threat_model>

<verification>
<automated>python -m pytest -q --basetemp .pytest-b2-preflight tests/test_m5_fitness_functions.py tests/test_s1_2_outbox_reconciliation.py; python scripts/classify_legacy_outbox.py --evidence artifacts/b2-rollout/01-preflight-handoff.json; python -m dbt test --project-dir dbt --profiles-dir dbt</automated>
</verification>

<success_criteria>
The preflight gate is green only when the recorded values are SAFE_STALE=0, LIVE_POST_MIGRATION=255, IN_FLIGHT_BLOCKED=0, BLOCKED=0, Bronze/Silver=218961/218961, Silver equals the accepted B2 projection, and dbt=26/26 PASS; the runtime remains legacy/legacy/0 until Plan 02 begins.
</success_criteria>

<output>
Create .planning/phases/01-b2-controlled-rollout/01-01-SUMMARY.md when done.
</output>
