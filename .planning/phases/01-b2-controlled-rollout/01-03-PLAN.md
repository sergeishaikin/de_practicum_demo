---
phase: 01-b2-controlled-rollout
plan: 03
type: execute
wave: 3
depends_on: [01-02C]
files_modified:
  - artifacts/b2-rollout/03-before.json
  - artifacts/b2-rollout/03-live-manifests-before.txt
  - artifacts/b2-rollout/03-drain-receipt.json
  - artifacts/b2-rollout/03-progress-final.json
  - artifacts/b2-rollout/03-dbt.log
autonomous: true
requirements: [CAN-02, CAN-03]
must_haves:
  truths:
    - "All 255 legitimate post-migration manifests are processed by B2 with no unresolved progress, FF-14 conflicts, or shadow mismatches."
    - "The exact pre-drain LIVE_POST_MIGRATION manifest set is durably captured and no post-migration manifest is silently omitted."
    - "Bronze/Silver correctness and dbt 26/26 remain green after the drain."
  artifacts:
    - path: "artifacts/b2-rollout/03-live-manifests-before.txt"
      provides: "Sorted identity list of the 255 legitimate manifests before drain"
      contains: "255 manifest paths"
    - path: "artifacts/b2-rollout/03-drain-receipt.json"
      provides: "Post-drain classifier and correctness receipt"
      contains: "LIVE_POST_MIGRATION=0, IN_FLIGHT_BLOCKED=0, BLOCKED=0"
    - path: "artifacts/b2-rollout/03-progress-final.json"
      provides: "Durable final medallion progress state"
      contains: "work is empty"
  key_links:
    - from: "artifacts/b2-rollout/03-live-manifests-before.txt"
      to: "artifacts/b2-rollout/03-drain-receipt.json"
      via: "before/after manifest identity comparison"
      pattern: "LIVE_POST_MIGRATION"
    - from: "scripts/classify_legacy_outbox.py"
      to: "iceberg/medallion/iceberg_medallion.py"
      via: "classifier reads outbox, progress, Bronze, and Silver after B2 commits"
      pattern: "--evidence artifacts/b2-rollout/03-drain-receipt.json"
---

<objective>
Drain and verify all 255 legitimate post-migration outbox manifests under the active B2/legacy/shadow canary.

Purpose: Prove the canary handles the entire accepted handoff workload, not only an isolated smoke cycle, while preserving the authoritative current-state invariants.
Output: Exact before-set, final drain receipt, progress capture, and dbt validation output.
</objective>

<execution_context>
@C:/Users/serge/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/serge/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-b2-controlled-rollout/CONTEXT.md
@.planning/phases/01-b2-controlled-rollout/01-02-PLAN.md
@artifacts/b2-rollout/01-preflight-handoff.json
@artifacts/b2-rollout/02-canary-receipt.json
@docs/remediation/S1.2-legacy-outbox-handoff.md
@iceberg/medallion/legacy_outbox_reconciliation.py
@iceberg/medallion/iceberg_medallion.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Freeze and identify the complete 255-manifest drain set</name>
  <files>artifacts/b2-rollout/03-live-manifests-before.txt</files>
  <action>Require the canary receipt to show b2/legacy/1 and a passing shadow cycle. Run python scripts/classify_legacy_outbox.py --evidence artifacts/b2-rollout/03-before.json using the active catalog variables, then parse the receipt and write a sorted list of every dispositions[].manifest whose status is LIVE_POST_MIGRATION to artifacts/b2-rollout/03-live-manifests-before.txt. Assert the receipt has exactly 255 LIVE_POST_MIGRATION entries, SAFE_STALE=0, IN_FLIGHT_BLOCKED=0, BLOCKED=0, and no active progress entry. Preserve this identity list for the after comparison; do not classify any of these manifests as stale and do not run cleanup_legacy_outbox.py because the 255 objects are legitimate work.</action>
  <verify>
    <automated>python scripts/classify_legacy_outbox.py --evidence artifacts/b2-rollout/03-before.json; python -c "import json; p=json.load(open('artifacts/b2-rollout/03-before.json')); assert p['live_post_migration']==255 and p['safe_stale']==0 and p['in_flight_blocked']==0 and p['blocked']==0; assert len([d for d in p['dispositions'] if d['status']=='LIVE_POST_MIGRATION'])==255; print('255-manifest freeze PASS')"</automated>
  </verify>
  <done>The exact 255-manifest set is captured, the handoff has no stale or blocked work, and no cleanup or data deletion command has run.</done>
</task>

<task type="auto">
  <name>Task 2: Drain the set and prove final correctness</name>
  <files>artifacts/b2-rollout/03-drain-receipt.json, artifacts/b2-rollout/03-progress-final.json, artifacts/b2-rollout/03-dbt.log</files>
  <action>Leave the existing b2/legacy/1 medallion service running and poll the read-only classifier until LIVE_POST_MIGRATION=0, with a bounded 45-minute execution window and a 15-second poll interval. Capture the final classifier as 03-drain-receipt.json. Copy the final MEDALLION_PROGRESS_PATH object with the existing Python environment using python -c "from pathlib import Path; from medallion.iceberg_medallion import get_fs; fs=get_fs(); Path('artifacts/b2-rollout/03-progress-final.json').write_text(fs.open_input_file('de-practicum/streaming/medallion/progress.json').read().decode('utf-8'))". Compare the final live manifest identities with 03-live-manifests-before.txt; require every pre-drain identity to be absent only because its B2 work completed and its progress entry is completed or pruned by the existing protocol. Require final SAFE_STALE=0, LIVE_POST_MIGRATION=0, IN_FLIGHT_BLOCKED=0, BLOCKED=0, Bronze rows=218961, Silver rows=218961, Bronze/Silver NULL business_version=0/0, unique Silver order IDs=true, and Silver equals the accepted B2 projection=true. Run python -m dbt test --project-dir dbt --profiles-dir dbt and capture its 26/26 PASS output in 03-dbt.log. On shadow mismatch, FF-14, recovery anomaly, stuck progress, timeout, correctness drift, or dbt failure, stop the medallion, set SILVER_MODE=legacy, GOLD_SOURCE=legacy, SHADOW_COMPARE=0, and run docker compose --env-file .env -f docker-compose.yml -f docker-compose.extended.yml up -d --no-deps --force-recreate iceberg-medallion; preserve all receipts and do not delete progress or reset tables.</action>
  <verify>
    <automated>python scripts/classify_legacy_outbox.py --evidence artifacts/b2-rollout/03-drain-receipt.json; python -c "import json; p=json.load(open('artifacts/b2-rollout/03-drain-receipt.json')); assert (p['safe_stale'],p['live_post_migration'],p['in_flight_blocked'],p['blocked'])==(0,0,0,0); assert (p['authoritative_bronze_rows'],p['authoritative_silver_rows'])==(218961,218961); assert p['silver_equals_b2_projection'] is True; print('drain correctness gate PASS')"; python -m dbt test --project-dir dbt --profiles-dir dbt</automated>
  </verify>
  <done>All 255 pre-drain manifests are drained, final work is not unresolved, B2 projection correctness is unchanged, and dbt remains 26/26 PASS; any failure has restored legacy configuration and a preserved failure receipt.</done>
</task>

<task type="auto">
  <name>Task 3: Assert complete successful manifest accounting</name>
  <files>artifacts/b2-rollout/03-drain-receipt.json</files>
  <action>After the normal drain, enrich the receipt with completed_manifest_ids equal to the 255 identities in 03-live-manifests-before.txt and failed_retries as an explicit empty list. A missing completion, failed retry, abandoned retry, or identity mismatch fails the plan even when LIVE_POST_MIGRATION is zero.</action>
  <verify><automated>python -c "import json; p=json.load(open('artifacts/b2-rollout/03-drain-receipt.json')); assert len(p['completed_manifest_ids'])==255; assert not p['failed_retries']; print('complete manifest accounting PASS')"</automated></verify>
  <done>All 255 legitimate manifests have explicit successful completion evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Outbox manifests -> B2 reader | Manifest paths and Bronze files determine work to process. |
| B2 progress -> subsequent polling | Durable progress controls retry and completion semantics. |
| B2 Silver -> legacy Gold shadow | Two logical projections must agree before Gold is written. |
| Drain receipts -> M5 gate | Counts and invariants authorize the next irreversible transition. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | Manifest classification | mitigate | Freeze and compare the exact 255 identity set; never use the S1.2 stale cleanup path. |
| T-01-02 | Repudiation | Progress completion | mitigate | Preserve the final progress object and require classifier/progress agreement before success. |
| T-01-03 | Denial of Service | Stuck B2 drain | mitigate | Use bounded polling, inspect in-flight state, and restore legacy configuration on timeout. |
| T-01-04 | Tampering | Silver current state | mitigate | Require Silver equals the accepted B2 projection, row counts, uniqueness, and null-version checks. |
| T-01-SC | Tampering | Package installation | accept | No package installation is planned; unexpected installation blocks the rollout. |
</threat_model>

<verification>
<automated>python -m pytest -q --basetemp .pytest-b2-drain tests/test_m5_fitness_functions.py tests/test_b2_medallion.py tests/test_s1_2_outbox_reconciliation.py; python scripts/classify_legacy_outbox.py --evidence artifacts/b2-rollout/03-drain-receipt.json; python -m dbt test --project-dir dbt --profiles-dir dbt</automated>
</verification>

<success_criteria>
The 255 legitimate manifests are fully drained under b2/legacy/1; final SAFE_STALE/LIVE_POST_MIGRATION/IN_FLIGHT_BLOCKED/BLOCKED are 0/0/0/0, Bronze/Silver is 218961/218961, Silver equals the accepted B2 projection, and dbt is 26/26 PASS.
</success_criteria>

<output>
Create .planning/phases/01-b2-controlled-rollout/01-03-SUMMARY.md when done.
</output>
