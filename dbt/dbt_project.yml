name: lakehouse_semantic
version: "1.0.0"
config-version: 2

profile: lakehouse

model-paths: [models]
test-paths: [tests]
macro-paths: [macros]
docs-paths: [docs]
target-path: target
clean-targets: [target, dbt_packages]

models:
  lakehouse_semantic:
    semantic:
      +materialized: view
