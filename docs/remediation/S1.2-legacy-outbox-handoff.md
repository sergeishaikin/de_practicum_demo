# S1.2 — legacy outbox handoff reconciliation

Status: **S1.2A classifier implemented; cleanup not approved**.

S1.1 migrated the authoritative Bronze and reconciled Silver through the
accepted B2 projection, but it did not remove pre-M1 Bronze outbox manifests.
Those manifests are work-control markers, not Bronze data. A stale marker can
therefore make a restarted B2 processor rediscover already-migrated physical
files without implying data corruption.

## S1.2A dry-run contract

`iceberg/medallion/legacy_outbox_reconciliation.py` is a read-only classifier.
It does not delete manifests, write Iceberg tables, write parquet files, or
write medallion progress. It classifies every manifest as `SAFE_STALE` or
`BLOCKED` only after checking:

- the manifest load-id is present in Bronze snapshot history;
- its append snapshot predates the S1.1 migration boundary;
- its physical rows are consumed exactly once from authoritative Bronze;
- its order IDs resolve to the current authoritative B2 state in Silver; and
- no active/in-flight progress entry references the manifest or its files.

The accepted legacy singleton rule is used only to normalize a NULL physical
version to `business_version=1` for comparison. The classifier never derives
business order from Kafka offset, partition, event time, or file order.

The cleanup digest covers only the exact manifest object paths classified as
`SAFE_STALE`. Cleanup is a separate operation requiring an explicit approval;
this phase intentionally contains no apply/delete flag.

## Evidence

The live receipt is generated with:

```powershell
$env:ICEBERG_CATALOG_URI = 'http://localhost:18181'
$env:S3_ENDPOINT = 'http://localhost:19000'
$env:AWS_ACCESS_KEY_ID = 'minio'
$env:AWS_SECRET_ACCESS_KEY = 'minio123'
python scripts/classify_legacy_outbox.py --evidence artifacts/s1.2a-dry-run.json
```

The receipt must be reviewed for `BLOCKED=0`, exact manifest/row counts,
authoritative snapshot IDs, the stable cleanup digest, and unchanged B2
projection evidence before any cleanup approval is considered.

## Current dry-run result

The persistent-stack dry run was completed on 2026-08-09 with the stack
restored to the legacy serving configuration. It classified all observed
manifests and found the expected physical counts:

| Evidence | Result |
| --- | ---: |
| manifests classified | 395 |
| logical rows | 14,870 |
| legacy NULL physical rows | 4,128 |
| versioned physical rows | 10,742 |
| authoritative Bronze NULL versions | 0 |
| authoritative Bronze rows | 218,961 |
| authoritative Silver rows | 218,961 |
| Silver equals B2 projection | true |

The disposition is intentionally **not cleanup-ready**:

| Disposition | Count |
| --- | ---: |
| `SAFE_STALE` | 139 |
| `BLOCKED` | 256 |

Blocked reasons are:

- 255 manifests are post-migration work and therefore cannot be treated as
  S1.1 legacy handoff state;
- 1 manifest (`00833e23bddc4c428692642b37891afa`) is referenced by active
  in-flight medallion progress.

The proposed 139-manifest cleanup set has stable digest
`991d8598930fa5410fcfdd3cbc8df5cb58d9068e1bc90df79286f5dc5af0194d`.
The exact load-id/object-path list is in
`artifacts/s1.2a-dry-run.json`. No manifest, parquet file, Iceberg table, or
progress state was modified. Because `BLOCKED` is non-zero, there is no
cleanup approval or S1.2B action.
